apiVersion: v1
kind: Service
metadata:
  name: agentgateway
  namespace: agentgateway
spec:
  type: ClusterIP
  selector:
    app: agentgateway
  ports:
    - name: http
      port: 8080
      targetPort: http
